## 1. Serialization Logic
- [x] 1.1 Implement `exportState()` and `importState(json)` in `ChessGame`.
- [x] 1.2 Implement `exportState()` and `importState(json)` in `LOAGame`.
- [x] 1.3 Implement `exportState()` and `importState(json)` in `XiangqiGame`.

## 2. UI Integration
- [x] 2.1 Add "Save" and "Load" buttons to `ChessUI` controls.
- [x] 2.2 Implement "Save" click handler: Generate JSON blob and trigger download with timestamped filename.
- [x] 2.3 Implement "Load" click handler: Trigger file input, read JSON, and restore game state.
- [x] 2.4 Repeat 2.1-2.3 for `LOAUI`.
- [x] 2.5 Repeat 2.1-2.3 for `XiangqiUI`.
